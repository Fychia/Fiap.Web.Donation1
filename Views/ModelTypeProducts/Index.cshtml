@model IEnumerable<Fiap.Web.Donation1.Models.ModelTypeProduct>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TypeProductName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TypeProductDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TypeProductName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TypeProductDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TypeProductID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TypeProductID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TypeProductID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
